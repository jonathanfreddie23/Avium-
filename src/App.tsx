import React from 'react';

import AppRoot from 'containers/AppRoot';

const App = (): JSX.Element => {
    return (
        <div className='App'>
            <AppRoot />
        </div>
    );
};

export default App;
